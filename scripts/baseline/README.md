# Baseline (2026-09-16)

Screenshots and physics values of the original eight labs, taken before the registry split.

`values.json` is the merge gate: after the split, `node scripts/smoke.mjs` must reproduce these numbers within the tolerances in that script. Chrome (exam tabs, hash URLs) is allowed to look different; the 3D scenes and live equations must not.
